import { createStyles } from "@mantine/core";

export const useCookieStyles = createStyles({
  styledBtn: {
    border: "2px solid",
  },
  styledSpan: {
    color: "#228be6",
    cursor: "pointer",
    ":hover": {
      textDecoration: "underline",
    },
  },
});
